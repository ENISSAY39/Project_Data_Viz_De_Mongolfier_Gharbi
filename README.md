# Project_Data_Viz_De_Mongolfier_Ghabi_LoL_Dataset

## Step 2 - Context and data assessment

### **1\. Context of the Study**

The dataset under analysis appears to relate to a sports league (based on the file name _league\_data.csv_). The objective is likely to explore team performance, compare results, and identify patterns or relationships between variables such as wins, losses, scores, or rankings.

Understanding the context is essential before performing any visualization or modeling. In this case, the data may represent seasonal performance metrics for multiple teams, possibly across several matches or time periods.

### **2\. Structure of the Dataset**

The dataset is assumed to be structured in a tabular format where:

-   Each **row** represents an observation (e.g., a team, a match, or a season entry)
-   Each **column** represents a variable (e.g., team name, number of wins, goals scored, etc.)

Typical variable types may include:

-   **Categorical variables**: team names, league divisions
-   **Numerical variables**: points, goals scored, goals conceded
-   **Temporal variables** (if present): match dates or seasons

### **3\. Data Quality Assessment**

Before analysis, it is important to evaluate data quality:

-   **Missing values**: Check if some entries are incomplete
-   **Outliers**: Identify unusually high or low values (e.g., extreme scores)
-   **Consistency**: Ensure uniform naming (e.g., team names not duplicated with different spellings)
-   **Data types**: Verify that each column has the correct format (numeric, categorical, date)

### **4\. Initial Observations**

From a preliminary inspection, the dataset likely contains:

-   Performance indicators that can be aggregated or compared
-   Variables suitable for ranking and correlation analysis
-   Potential relationships between offensive and defensive metrics

These characteristics make the dataset appropriate for various types of visualizations such as comparisons, distributions, and trend analysis.

### **5\. Analytical Potential**

The dataset allows for several analytical directions:

-   Comparing team performance across metrics
-   Identifying trends over time (if temporal data exists)
-   Detecting correlations (e.g., between goals scored and wins)
-   Highlighting variability and outliers in performance
